# ローカル開発用の環境変数サンプル
# このファイルを .env.local にコピーして使用してください
# cp .env.local.example .env.local

# ─────────────────────────────────────────────
# ローカル認証モード
# true にするとCognito不要でダミーユーザーとしてログイン済み状態になります
# ─────────────────────────────────────────────
VITE_LOCAL_AUTH=true

# ダミーユーザーのカスタマイズ（任意）
# VITE_LOCAL_AUTH_NAME=ローカルユーザー
# VITE_LOCAL_AUTH_EMAIL=local@example.com

# ─────────────────────────────────────────────
# バックエンドAPI (Cloudflare Workers / wrangler dev で起動したURL)
# ─────────────────────────────────────────────
# BACKEND_API=http://localhost:8787

# ─────────────────────────────────────────────
# Cognito設定 (VITE_LOCAL_AUTH=false の場合のみ必要)
# ─────────────────────────────────────────────
# VITE_COGNITO_AUTHORITY_URL=https://cognito-idp.ap-northeast-1.amazonaws.com/<your-user-pool-id>
# VITE_COGNITO_CLIENT_ID=<your-client-id>
# VITE_COGNITO_DOMAIN=https://<your-domain>.auth.ap-northeast-1.amazoncognito.com
# VITE_REDIRECT_URI=http://localhost:3000
